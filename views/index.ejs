<html>
  <head>
    <title>
      <%= title%>
    </title>
  </head>
  <body>
  <a href="/new"> New Message</a>
   <ul>
      <% messages.forEach((message) => { %>
      <%- include('messages', {text: message.text, user: message.user, added: message.added}); %>
      <% }); %>
  </ul>
  </body>
</html>
